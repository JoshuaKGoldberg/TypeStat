{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Debug Mutation Tests",
            "preLaunchTask": "tsc",
            "program": "${workspaceRoot}/node_modules/mocha/bin/_mocha",
            "args": ["--reporter", "spec", "--colors", "--no-timeouts", "${workspaceFolder}/src/tests/runTests.js"]
        }
    ]
}
